import React from "react";

const base = (props) => {
 
    return(
        <>
            <div className="base">
                <span> {props.prodname} </span>    
            </div>
        </>
    );
}
export default base;
